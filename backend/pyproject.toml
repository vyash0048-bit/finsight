[project]
name = "finsight-backend"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "strict"
testpaths = ["tests"]

[tool.ruff]
lint.ignore = ["B008", "BLE001", "DTZ005", "RUF013", "F841", "S110", "TRY002", "SIM102", "E501"]
